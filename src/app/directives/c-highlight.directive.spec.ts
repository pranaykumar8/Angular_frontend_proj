import { CHighlightDirective } from './c-highlight.directive';

describe('CHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
